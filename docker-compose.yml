version: '3.8'
services:
  bot:
    build: .
    container_name: sylenth-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./downloads:/app/downloads
      - ./sylenth.db:/app/sylenth.db
